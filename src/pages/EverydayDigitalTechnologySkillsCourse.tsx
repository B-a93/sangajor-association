import { useEffect, useMemo, useState } from 'react';
import { Award, CheckCircle2, Circle, LockKeyhole } from 'lucide-react';
import { EVERYDAY_DIGITAL_TECHNOLOGY_PASSING_SCORE as PASSING_SCORE, EVERYDAY_DIGITAL_TECHNOLOGY_SKILLS_SLUG as COURSE_SLUG, everydayDigitalTechnologyFinalQuestions as finalQuestions, everydayDigitalTechnologySkillsLessons as lessons, type DigitalSkillsQuestion } from '../data/everydayDigitalTechnologySkillsCourse';
import { supabase } from '../lib/supabase';
import './DigitalIncomeLesson.css';

type Progress = { lesson_slug:string; read_sections:string[]; pathway_answers:{assignment?:string}; knowledge_answers:Record<string,string>; knowledge_score:number; knowledge_submitted:boolean; completed_at:string|null };
const slug=(n:number)=>`everyday-digital-technology-skills-lesson-${n}`;
const base='#/dashboard/learning/everyday-digital-technology-skills';
const errorText=(action:string,message:string)=>import.meta.env.DEV?`${action}: ${message}`:`${action}. Please try again.`;
const score=(questions:DigitalSkillsQuestion[],answers:Record<string,string>)=>Math.round(questions.filter(q=>Number(answers[q.id])===q.answer).length*100/questions.length);
async function member(){const {data}=await supabase.auth.getSession();if(!data.session)window.location.hash='/login';return data.session?.user.id??''}

export function EverydayDigitalTechnologySkillsDashboard(){
 const [rows,setRows]=useState<Progress[]>([]),[assignment,setAssignment]=useState<any>(),[assessment,setAssessment]=useState<any>(),[certificate,setCertificate]=useState<any>();const [loading,setLoading]=useState(true);
 useEffect(()=>{void(async()=>{const id=await member();if(!id)return;await supabase.from('course_enrolments').upsert({member_id:id,course_slug:COURSE_SLUG},{onConflict:'member_id,course_slug'});const [p,a,f,c]=await Promise.all([supabase.from('lesson_progress').select('*').eq('member_id',id).like('lesson_slug','everyday-digital-technology-skills-lesson-%'),supabase.from('course_assignments').select('*').eq('member_id',id).eq('course_slug',COURSE_SLUG).maybeSingle(),supabase.from('course_assessments').select('*').eq('member_id',id).eq('course_slug',COURSE_SLUG).maybeSingle(),supabase.from('learning_certificates').select('*').eq('member_id',id).eq('course_slug',COURSE_SLUG).maybeSingle()]);setRows((p.data as Progress[])??[]);setAssignment(a.data);setAssessment(f.data);setCertificate(c.data);setLoading(false)})()},[]);
 if(loading)return <section className="lesson-state">Loading your course…</section>;const completed=rows.filter(r=>r.completed_at).length,percent=Math.round(completed/6*100),all=completed===6,first=lessons.find(l=>!rows.find(r=>r.lesson_slug===slug(l.number))?.completed_at)?.number??6;
 return <section className="lesson-page course-dashboard"><header className="lesson-hero"><div><p className="eyebrow">Digital &amp; Technology Skills</p><h1>Everyday Digital &amp; Technology Skills</h1><p>Build the confidence to use smartphones, computers, email, the internet, digital documents, online communication and introductory AI tools safely and effectively.</p></div>{certificate?.status==='Approved'&&<span className="course-complete"><Award/> Course Completed</span>}</header><aside className="lesson-progress"><div><strong>{percent}% overall progress</strong><span>{completed} of 6 lessons completed</span></div><progress max="100" value={percent}/><a className="primary-button" href={`${base}/lesson-${first}`}>Continue Learning</a></aside>
 <div className="course-lesson-list">{lessons.map(l=>{const prior=l.number===1||Boolean(rows.find(r=>r.lesson_slug===slug(l.number-1))?.completed_at),row=rows.find(r=>r.lesson_slug===slug(l.number)),done=Boolean(row?.completed_at);return <article key={l.number} className={`course-row ${done?'done':prior?'available':'locked'}`}>{done?<CheckCircle2/>:prior?<Circle/>:<LockKeyhole/>}<div><small>Lesson {l.number}</small><h2>{l.title}</h2><p>{done?`Completed · Score ${row?.knowledge_score}%`:prior?(row?'In progress':'Available'):`Locked until Lesson ${l.number-1} is complete`}</p></div>{prior&&<a href={`${base}/lesson-${l.number}`}>{done?'Review':'Open'}</a>}</article>})}</div>
 <section className="certificate-panel"><h2>Final assessment &amp; practical assignment</h2><ul><li className={all?'done':''}>Complete all six sequential lessons</li><li className={assessment?.score>=70?'done':''}>Pass the final assessment at 70% or higher {assessment&&`(${assessment.score}%)`}</li><li className={assignment?.status==='approved'?'done':''}>Receive tutor verification for the practical assignment {assignment&&`(${assignment.status})`}</li></ul>{all?<a className="primary-button" href={`${base}/final-assessment`}>Open Final Assessment</a>:<p><LockKeyhole size={18}/> Unlocks after Lesson 6.</p>}{certificate?.status==='Pending Chairman Approval'&&<p className="quiz-result pass">Tutor verified. Certificate is Pending Chairman Approval.</p>}{certificate?.status==='Approved'&&<p className="quiz-result pass"><Award/> Certificate of Completion in Everyday Digital &amp; Technology Skills: {certificate.certificate_number}</p>}{certificate?.status==='Rejected'&&<p><strong>Certificate rejected:</strong> {certificate.rejection_reason}</p>}</section></section>
}

export function EverydayDigitalTechnologySkillsLesson({lessonNumber}:{lessonNumber:number}){
 const lesson=lessons[lessonNumber-1];const [memberId,setMemberId]=useState(''),[started,setStarted]=useState(false),[read,setRead]=useState<string[]>([]),[activity,setActivity]=useState(''),[answers,setAnswers]=useState<Record<string,string>>({}),[submitted,setSubmitted]=useState<number|null>(null),[completed,setCompleted]=useState(false),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[notice,setNotice]=useState('');
 useEffect(()=>{void(async()=>{const id=await member();if(!id)return;setMemberId(id);if(lessonNumber>1){const {data}=await supabase.from('lesson_progress').select('completed_at').eq('member_id',id).eq('lesson_slug',slug(lessonNumber-1)).maybeSingle();if(!data?.completed_at){window.location.hash=base;return}}const {data}=await supabase.from('lesson_progress').select('*').eq('member_id',id).eq('lesson_slug',slug(lessonNumber)).maybeSingle();if(data){setStarted(true);setRead(data.read_sections??[]);setActivity(data.pathway_answers?.assignment??'');setAnswers(data.knowledge_answers??{});setSubmitted(data.knowledge_submitted?data.knowledge_score:null);setCompleted(Boolean(data.completed_at))}setLoading(false)})()},[lessonNumber]);
 const localScore=useMemo(()=>score(lesson.questions,answers),[lesson,answers]),passed=submitted!==null&&submitted>=PASSING_SCORE,ready=lesson.sections.every(s=>read.includes(s.id))&&activity.trim().length>=40&&passed;
 async function save(message='Progress saved. Continue later from any signed-in device.'){setSaving(true);const {error}=await supabase.from('lesson_progress').upsert({member_id:memberId,lesson_slug:slug(lessonNumber),read_sections:read,pathway_answers:{assignment:activity},knowledge_answers:answers,knowledge_score:submitted??0,knowledge_submitted:submitted!==null,updated_at:new Date().toISOString()},{onConflict:'member_id,lesson_slug'});setSaving(false);setNotice(error?errorText('Progress could not be saved',error.message):message);return !error}
 async function submit(){if(Object.keys(answers).length!==6){setNotice('Answer all six questions before submitting.');return}setSaving(true);const {data,error}=await supabase.rpc('submit_everyday_digital_technology_knowledge_check',{lesson_number:lessonNumber,submitted_answers:answers});setSaving(false);if(error)setNotice(errorText('Knowledge check could not be submitted',error.message));else{const value=Number(data??localScore);setSubmitted(value);setNotice(value>=70?`Knowledge check passed with ${value}%.`:`You scored ${value}%. Review and retry; 70% is required.`)}}
 async function complete(){if(!ready)return;setSaving(true);const {error}=await supabase.rpc('complete_everyday_digital_technology_lesson',{lesson_number:lessonNumber,section_ids:read,activity_response:activity});setSaving(false);if(error)setNotice(errorText('Completion could not be recorded',error.message));else{setCompleted(true);setNotice(`Lesson ${lessonNumber} complete. ${lessonNumber<6?`Lesson ${lessonNumber+1}`:'The Final Assessment'} is unlocked.`)}}
 if(loading)return <section className="lesson-state">Checking lesson access…</section>;return <section className="lesson-page"><header className="lesson-hero"><div><p className="eyebrow">Everyday Digital &amp; Technology Skills</p><h1>Lesson {lessonNumber}: {lesson.title}</h1><p>{lesson.introduction}</p></div><a className="secondary-button" href={base}>Course dashboard</a></header>{notice&&<p className="lesson-notice" role="status">{notice}</p>}{!started?<section className="lesson-start"><h2>Ready to begin?</h2><p>Progress is securely saved to your member account and survives refresh and sign-in.</p><button className="primary-button" onClick={()=>void save('Lesson started.').then(ok=>ok&&setStarted(true))}>Start Lesson</button></section>:<><main className="lesson-content"><section className="lesson-objectives"><h2>Learning objectives</h2><ul>{lesson.objectives.map(o=><li key={o}>{o}</li>)}</ul></section>{lesson.sections.map((s,i)=><article className="lesson-section" key={s.id}><span className="section-number">{i+1}</span><div><h2>{s.title}</h2><p>{s.body}</p><p className="practical-example"><strong>Practical example:</strong> {s.example}</p><label className="read-check"><input type="checkbox" checked={read.includes(s.id)} onChange={e=>setRead(e.target.checked?[...read,s.id]:read.filter(x=>x!==s.id))}/> I have read and understood this section</label></div></article>)}<section className="lesson-activity"><p className="eyebrow">Practical activity</p><h2>{lesson.activity.prompt}</h2><p>{lesson.activity.hint}</p><label>Your response (minimum 40 characters)<textarea rows={7} maxLength={3000} value={activity} onChange={e=>setActivity(e.target.value)}/></label></section><fieldset className="knowledge-check"><legend>Six-question knowledge check</legend><p>Passing requirement: 70%. Review and retry if needed.</p>{lesson.questions.map((q,i)=><fieldset key={q.id}><legend>{i+1}. {q.prompt}</legend>{q.options.map((o,j)=><label key={o}><input type="radio" name={q.id} checked={answers[q.id]===String(j)} onChange={()=>{setAnswers({...answers,[q.id]:String(j)});setSubmitted(null)}}/>{o}</label>)}</fieldset>)}<button className="primary-button" disabled={saving} onClick={()=>void submit()}>Submit Knowledge Check</button>{submitted!==null&&<p className={passed?'quiz-result pass':'quiz-result'}>Score: {submitted}% — {passed?'Passed':'Not yet passed'}</p>}</fieldset></main><div className="lesson-actions"><button className="secondary-button" disabled={saving} onClick={()=>void save()}>Save and Continue Later</button><button className="primary-button" disabled={!ready||completed||saving} onClick={()=>void complete()}>{completed?'Lesson Completed':'Complete Lesson'}</button></div></>}<nav className="lesson-navigation" aria-label="Lesson navigation">{lessonNumber===1?<a href={base}>← Previous Lesson</a>:<a href={`${base}/lesson-${lessonNumber-1}`}>← Previous Lesson</a>}{completed?<a href={lessonNumber===6?`${base}/final-assessment`:`${base}/lesson-${lessonNumber+1}`}>Next Lesson →</a>:<span aria-disabled="true"><LockKeyhole size={16}/> Next Lesson locked</span>}</nav></section>
}

export function EverydayDigitalTechnologySkillsFinalAssessment(){const [answers,setAnswers]=useState<Record<string,string>>({}),[result,setResult]=useState<number|null>(null),[assignment,setAssignment]=useState(''),[status,setStatus]=useState(''),[memberId,setMemberId]=useState(''),[loading,setLoading]=useState(true),[notice,setNotice]=useState('');useEffect(()=>{void(async()=>{const id=await member();if(!id)return;setMemberId(id);const p=await supabase.from('lesson_progress').select('lesson_slug').eq('member_id',id).not('completed_at','is',null).like('lesson_slug','everyday-digital-technology-skills-lesson-%');if((p.data?.length??0)<6){window.location.hash=base;return}const [a,f]=await Promise.all([supabase.from('course_assignments').select('*').eq('member_id',id).eq('course_slug',COURSE_SLUG).maybeSingle(),supabase.from('course_assessments').select('*').eq('member_id',id).eq('course_slug',COURSE_SLUG).maybeSingle()]);if(a.data){setAssignment(a.data.submission_text);setStatus(a.data.status)}if(f.data){setAnswers(f.data.answers);setResult(f.data.score)}setLoading(false)})()},[]);async function submitQuiz(){if(Object.keys(answers).length!==6){setNotice('Answer all six questions.');return}const {data,error}=await supabase.rpc('submit_everyday_digital_technology_final_assessment',{submitted_answers:answers});if(error)setNotice(error.message);else setResult(Number(data))}async function submitAssignment(){const {error}=await supabase.from('course_assignments').upsert({member_id:memberId,course_slug:COURSE_SLUG,submission_text:assignment,status:'submitted',submitted_at:new Date().toISOString()},{onConflict:'member_id,course_slug'});if(error)setNotice(error.message);else setStatus('submitted')}if(loading)return <section className="lesson-state">Checking assessment access…</section>;return <section className="lesson-page"><header className="lesson-hero"><div><p className="eyebrow">After Lesson 6</p><h1>Final Assessment &amp; Practical Assignment</h1><p>Demonstrate safe, responsible and productive use of everyday technology.</p></div></header>{notice&&<p role="status">{notice}</p>}<fieldset className="knowledge-check"><legend>Final Assessment</legend><p>Six questions; 70% is required.</p>{finalQuestions.map((q,i)=><fieldset key={q.id}><legend>{i+1}. {q.prompt}</legend>{q.options.map((o,j)=><label key={o}><input type="radio" name={q.id} checked={answers[q.id]===String(j)} onChange={()=>setAnswers({...answers,[q.id]:String(j)})}/>{o}</label>)}</fieldset>)}<button className="primary-button" onClick={()=>void submitQuiz()}>Submit Final Assessment</button>{result!==null&&<p className={result>=70?'quiz-result pass':'quiz-result'}>Score: {result}% — {result>=70?'Passed':'Review and retry'}</p>}</fieldset><section className="lesson-activity"><h2>Practical assignment: My safe digital workflow</h2><p>Write a realistic plan showing device choice, a professional email, file structure and backup, collaboration steps, safety checks, and a privacy-safe AI prompt with a fact-checking method.</p><label>Your complete workflow (minimum 200 characters)<textarea rows={14} minLength={200} maxLength={10000} value={assignment} onChange={e=>setAssignment(e.target.value)}/></label><button className="primary-button" disabled={assignment.trim().length<200||status==='approved'} onClick={()=>void submitAssignment()}>{status==='submitted'?'Resubmit Practical Assignment':'Submit Practical Assignment'}</button>{status&&<p>Status: <strong>{status}</strong>. An approved tutor must verify this work before it moves to Pending Chairman Approval.</p>}</section><nav className="lesson-navigation"><a href={`${base}/lesson-6`}>← Previous Lesson</a><a href={base}>Course dashboard →</a></nav></section>}
